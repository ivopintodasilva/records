# CollectionFeature Demo App

This is a standalone demo app entry point for the `CollectionFeature` target.

## Usage
1. Create a new iOS App target in Xcode (or a small demo Xcode project).
2. Add the `RecordsKit` package as a dependency.
3. Use `CollectionFeatureDemoApp.swift` as the app entry point.
