# AppFeature Demo App

This is a standalone demo app entry point for the `AppFeature` target.

## Usage
1. Create a new iOS App target in Xcode (or a small demo Xcode project).
2. Add the `RecordsKit` package as a dependency.
3. Use `AppFeatureDemoApp.swift` as the app entry point.
